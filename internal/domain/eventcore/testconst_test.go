package eventcore

// unknownProvider is the literal used across tests for an invalid/unknown enum value.
const unknownProvider = "unknown"

// Shared test literals for the Brazil country fixture.
const (
	bra    = "BRA"
	flagBR = ":flag_br:"
	brazil = "Brazil"
)
